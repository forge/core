head<head(---)> 
key(What It Is)operator(:) string<content(YAML is a human friendly data serialization)content(
            standard for all programming languages.)>
head<head(---)> 
key(What It Is)operator(:) string<content(YAML is a human friendly data serialization)content(
 standard for all programming languages.)>
head<head(---)> 
key(What It Is)operator(:) string<content(YAML is a human friendly data serialization)>
error(standard for all programming languages.)
head<head(---)> 
operator(-) string<content(What It Is: YAML is a human friendly data serialization)content(
            standard for all programming languages.)>
head<head(---)> 
operator(-) string<content(What It Is: YAML is a human friendly data serialization)content(
   standard for all programming languages.)>
head<head(---)> 
operator(-) string<content(What It Is: YAML is a human friendly data serialization)>
  error(standard for all programming languages.)
head<head(---)> 
operator(-) string<content(What It Is: YAML is a human friendly data serialization)>
error(standard for all programming languages.)
